FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

WORKDIR /app

COPY requirements_line_bot.txt .
RUN pip install --no-cache-dir -r requirements_line_bot.txt

COPY . .

# Cloud Run 會提供 PORT，預設用 8080
CMD ["gunicorn", "-b", "0.0.0.0:8080", "app:app"]




